{
  "name": "flametracer-promo",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && type nul > dist/.nojekyll && git add dist && git commit -m 'Deploy to GitHub Pages' && git push origin HEAD:gh-pages --force"
  },
  "dependencies": {
    "astro": "^5.2.5"
  }
}
