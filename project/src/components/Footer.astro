<footer class="footer">
  <div class="container">
    <div class="footer-content">
      <div class="footer-brand">
        <a href="/" class="footer-logo">
          <img
            src="/assets/favicon.svg"
            alt="Flametracer"
            width="40"
            height="40"
          />
          <span>Flametracer</span>
        </a>
      </div>

      <div class="footer-links">
        <div class="footer-links-group">
          <h3>Navegação</h3>
          <ul>
            <li><a href="#features">Funcionalidades</a></li>
            <li><a href="#about">Sobre</a></li>
            <li><a href="#download">Descarregar</a></li>
            <li><a href="#contact">Contactos</a></li>
          </ul>
        </div>

        <div class="footer-links-group">
          <h3>Descarregar</h3>
          <div class="app-links">
            <a href="#download" class="download-link">Descarregar APK</a>
          </div>
        </div>
      </div>
    </div>

    <div class="footer-bottom">
      <p>© {new Date().getFullYear()} Flametracer. Todos os direitos reservados.</p>
      <p class="footer-credit">Feito com paixão pela Equipa Flametracer</p>
    </div>
  </div>

  <button id="back-to-top" aria-label="Voltar ao topo">
    <svg
      xmlns="http://www.w3.org/2000/svg"
      width="24"
      height="24"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      stroke-width="2"
      stroke-linecap="round"
      stroke-linejoin="round"
    >
      <polyline points="18 15 12 9 6 15"></polyline>
    </svg>
  </button>
</footer>

<script>
  // Back to top button
  const backToTopButton = document.getElementById("back-to-top");

  if (backToTopButton) {
    window.addEventListener("scroll", () => {
      if (window.scrollY > 300) {
        backToTopButton.classList.add("visible");
      } else {
        backToTopButton.classList.remove("visible");
      }
    });

    backToTopButton.addEventListener("click", () => {
      window.scrollTo({
        top: 0,
        behavior: "smooth",
      });
    });
  }
</script>

<style>
  .footer {
    background-color: rgba(15, 15, 15, 0.8);
    backdrop-filter: blur(10px);
    padding: var(--space-4) 0 var(--space-3); /* Reduced padding to make footer thinner */
    position: relative;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
  }

  .footer-content {
    display: flex;
    justify-content: space-between; /* Align brand left and links right */
    align-items: center; /* Vertically center content */
    gap: var(--space-5);
    margin-bottom: var(--space-3); /* Reduced margin */
  }

  .footer-brand {
    display: flex;
    align-items: center;
    min-height: 80px; /* Reduced height for thinner footer */
  }

  .footer-logo {
    display: flex;
    align-items: center;
    gap: var(--space-1);
    color: var(--color-text-primary);
    font-weight: 700;
    font-size: 1.5rem;
    text-decoration: none;
  }

  .footer-links {
    display: flex;
    gap: var(--space-5);
    align-items: center; /* Center links vertically */
  }

  .footer-links-group h3 {
    margin-bottom: var(--space-2);
    font-size: 1.125rem;
    color: var(--color-text-primary);
  }

  .footer-links-group ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .footer-links-group li {
    margin-bottom: var(--space-1);
  }

  .footer-links-group a {
    color: var(--color-text-secondary);
    text-decoration: none;
    transition: color var(--transition-fast);
    display: inline-block;
  }

  .footer-links-group a:hover {
    color: var(--color-primary-400);
    transform: translateX(3px);
  }

  .app-links {
    margin-top: var(--space-2);
  }

  .download-link {
    display: inline-block;
    padding: 8px 16px;
    background: rgba(255, 255, 255, 0.05);
    border-radius: var(--border-radius-sm);
    color: var(--color-text-primary);
    text-decoration: none;
    transition: background var(--transition-fast);
  }

  .download-link:hover {
    background: rgba(249, 115, 22, 0.2);
    color: var(--color-primary-400);
  }

  .footer-bottom {
    padding-top: var(--space-2); /* Reduced padding */
    border-top: 1px solid rgba(255, 255, 255, 0.05);
    display: flex;
    justify-content: space-between;
    align-items: center;
    color: var(--color-text-secondary);
    font-size: 0.875rem;
  }

  .footer-credit {
    opacity: 0.7;
  }

  #back-to-top {
    position: fixed;
    bottom: 20px; /* Adjusted to account for thinner footer */
    right: 30px;
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: linear-gradient(
      135deg,
      var(--color-primary-600),
      var(--color-secondary-700)
    );
    color: white;
    border: none;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    opacity: 0;
    visibility: hidden;
    transform: translateY(20px);
    transition:
      opacity var(--transition-fast),
      visibility var(--transition-fast),
      transform var(--transition-fast);
    box-shadow: 0 4px 12px rgba(249, 115, 22, 0.3);
  }

  #back-to-top.visible {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  #back-to-top:hover {
    transform: translateY(-5px);
    box-shadow: 0 6px 16px rgba(249, 115, 22, 0.4);
  }

  @media (max-width: 992px) {
    .footer-content {
      flex-direction: column; /* Stack vertically on smaller screens */
      gap: var(--space-3); /* Reduced gap */
      text-align: center;
    }

    .footer-brand {
      min-height: auto; /* Remove min-height for mobile */
    }

    .footer-logo {
      justify-content: center;
    }

    .footer-links {
      justify-content: center;
      flex-wrap: wrap;
    }

    .footer-links-group {
      width: 100%;
      max-width: 200px;
      margin: 0 auto;
    }
  }

  @media (max-width: 768px) {
    .footer-links {
      flex-direction: column;
      gap: var(--space-3); /* Reduced gap */
      text-align: center;
    }

    .app-links {
      justify-content: center;
    }

    .footer-bottom {
      flex-direction: column;
      text-align: center;
      gap: var(--space-1);
    }

    #back-to-top {
      bottom: 15px; /* Adjusted for thinner footer */
      right: 20px;
      width: 40px;
      height: 40px;
    }
  }
</style>